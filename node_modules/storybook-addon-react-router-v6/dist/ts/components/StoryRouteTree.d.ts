import React from "react";
import { ActionFunction, RouteProps } from "react-router-dom";
import { FCC } from "../fixes";
import { HydrationState, LoaderFunction } from "@remix-run/router";
type OutletProps = {
    element: React.ReactNode;
    path?: string;
    handle?: unknown;
    loader?: LoaderFunction;
    action?: ActionFunction;
    errorElement?: React.ReactNode | null;
};
export type StoryRouterProps = {
    browserPath?: string;
    routePath?: string;
    routeParams?: Record<string, string>;
    routeHandle?: unknown;
    searchParams?: ConstructorParameters<typeof URLSearchParams>[0];
    routeState?: unknown;
    outlet?: React.ReactNode | OutletProps;
    hydrationData?: HydrationState;
    loader?: LoaderFunction;
    action?: ActionFunction;
    errorElement?: React.ReactNode | null;
    shouldRevalidate?: RouteProps['shouldRevalidate'];
};
export declare const StoryRouteTree: FCC<StoryRouterProps>;
export {};
